### This file was generated by Nexus Schema
### Do not make changes to this file directly


type AuthPayload {
  message: String
  token: String
  user: User
}

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar DateTime

type Item {
  createdAt: DateTime!
  id: Int!
  name: String!
  type: TypeEnum!
  workspace: Workspace
}

type Mutation {
  addItem(data: addItemInput!, workspaceId: Int!): Item
  createWorkspace(authorEmail: String!, data: WorkspaceCreateInput!): Workspace
  deleteWorkspace(id: Int!): Workspace
  login(email: String!, password: String!): AuthPayload
  removeItem(id: Int!): Item
  shareWorkspace(data: WorkspaceShareInput!, workspaceId: Int!): Workspace
  signupUser(data: UserCreateInput!): User!
  unshareWorkspace(data: WorkspaceShareInput!, workspaceId: Int!): Workspace
  updateItem(data: updateItemInput!, itemId: Int!): Item
}

type Query {
  allUsers: [User!]!
  userDetails: User
  workspaceById(workspaceId: Int!): Workspace
}

enum TypeEnum {
  DONE
  FAIL
  TODO
}

type User {
  email: String!
  id: String!
  name: String
  owned: [Workspace!]!
  shared: [Workspace]
  verified: Boolean
}

input UserCreateInput {
  email: String!
  name: String
  password: String!
}

input UserUniqueInput {
  email: String
  id: Int
}

type Workspace {
  color: String!
  createdAt: DateTime!
  id: Int!
  items: [Item]!
  owner: User
  title: String!
  updatedAt: DateTime!
  users: [User]!
}

input WorkspaceCreateInput {
  color: String!
  title: String!
}

input WorkspaceShareInput {
  name: String!
}

input addItemInput {
  name: String!
}

input updateItemInput {
  type: TypeEnum!
}